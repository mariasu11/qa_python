[pytest]
addopts = -q --maxfail=1 --disable-warnings -n auto --html=report.html --self-contained-html
markers =
    smoke: health-check endpoints
    integration: real API workflows
    contract: JSON schema validation
    performance: latency/throughput checks